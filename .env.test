# Test environment configuration
# Used by spec_helper.rb and CI pipelines.
# No secrets — safe to commit.

LOG_LEVEL=fatal
# Enable escalation in tests so router specs exercise the full routing logic.
# The local .env may set ESCALATION_ENABLED=false for development; this
# override ensures tests are environment-independent.
ESCALATION_ENABLED=true
